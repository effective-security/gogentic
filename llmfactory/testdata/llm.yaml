---
providers:
  # the first provider is the default one
  - name: openai-dev
    token: env://OPENAI_API_KEY
    default_model: gpt-4o
    available_models:
      - gpt-4
      - gpt-4o
      - gpt-4-mini
    open_ai:
      # OPEN_AI|AZURE|ANTHROPIC|CLOUDFLARE_AZURE
      api_type: OPEN_AI
